/**
 * Web shell placeholder (Milestone 1).
 * No React / Babylon yet — those land after the greybox combat feel is proven.
 */

const el = document.getElementById('status');
if (el) {
  el.textContent = 'Aether Break — boot OK';
}

export {};
